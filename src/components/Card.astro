---
import FormattedDate from './helpers/FormattedDate.astro';
import { Image } from 'astro:assets';
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'blog'>;
}

const { post } = Astro.props;
const { id, data: postData } = post;
---

<a
  class="card"
  href={`/blog/${id}/`}
>
  <article class="card__layout">

      <div class="card__eyebrow">
        <Image
          src={postData.heroImage}
          alt=""
          fit="cover"
          position="center"
          width={40}
          height={40}
          class="card__image"
        />
        <FormattedDate date={postData.pubDate} />
      </div>

      <h2 class="card__title">
        {postData.title}
      </h2>

      <p class="card__desc">
        {postData.description}
      </p>

      <div class="card__tags" aria-label="Categories">
        {postData.categories.map((category) => (
            <span class="tag">
              <span class="tag__count">#</span>
              <span class="tag__label">{category}</span>
            </span>
        ))}
      </div>      

  </article>
</a>
