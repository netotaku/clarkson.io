<svg
  class="ripple-loader"
  width="192"
  height="192"
  viewBox="0 0 24 24"
  xmlns="http://www.w3.org/2000/svg"
  transition:persist="equaliser-loader"
  aria-hidden="true"
  focusable="false"
>
  <circle class="spinner_7NYg" cx="12" cy="12" r="0" />
</svg>

<style>
  .ripple-loader {
    --loader-opacity: 0.3;

    position: fixed;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    /* .main is a later sibling with a transform and an opaque background. */
    z-index: 0;
    color: rgb(255 255 255 / var(--loader-opacity));
    fill: currentColor;
    pointer-events: none;
  }

  .spinner_7NYg {
    animation: spinner_0KQs 1.2s cubic-bezier(0.52, .6, .25, .99) infinite;
  }

  @keyframes spinner_0KQs {
    0% { r: 0; opacity: 1; }
    100% { r: 11px; opacity: 0; }
  }

  @media (prefers-reduced-motion: reduce) {
    .spinner_7NYg {
      animation: none;
      r: 11px;
      opacity: 1;
    }
  }
</style>
