---
import Wrapper 	from '../layouts/wrapper.astro';
import Wurd from '../components/Wurd.vue';
import { HeartIcon } from '../icons';
---
<Wrapper
	title="Wurd"
	description="Play Wurd, a codeword puzzle that challenges you to reveal a hidden quotation before your lives run out."
	image='/src/assets/images/space.jpg'>
	
    <section class="section section--wurd">
        <div class="wurd">
            <div class="wurd__game">
                <Wurd
                    client:visible
                    endpoint="/.netlify/functions/quote"
                    startingLives={4}
                    starterVowels={2}
                    starterConsonants={2}
                    instancesPerStarter={2}
                    autoNextDelay={500}
                    useMockQuote={import.meta.env.DEV}
                >
                    <HeartIcon slot="lives-icon" />
                </Wurd>
            </div>
        </div>
    </section>
</Wrapper>
